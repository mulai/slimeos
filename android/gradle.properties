org.gradle.jvmargs=-Xmx4096M
android.useAndroidX=true
android.enableJetifier=false
android.nonTransitiveRClass=true
# AGP 9.2.1 has built-in Kotlin support that conflicts with an explicitly
# applied org.jetbrains.kotlin.android plugin — matches what the FreeRDP
# submodule's own gradle.properties sets for the same reason.
android.builtInKotlin=false
android.newDsl=false
