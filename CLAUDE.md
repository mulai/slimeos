# Slime OS

Repo conventions (commits, OTA version bumps, product rules): `docs/dev-conventions.md`.

Local sessions: first read `.claude/FUTURE_SESSION_NOTES.md` (gitignored, local only) for
whatever was left pending last time. Keep it lean: prune items once they're done.

Cloud sessions: that file and the local memory don't exist there, and there is no access to
devices, Brains or the hub. Skip it, work from the repo and GitHub issues, and finish with a PR.
